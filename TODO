1) Add support for Windows. OS dependent files are: logger.c and inet.c
2) Use new log*Str where applicable

3) BoolVector
4) create exception class which loggs file, function and line automatically

0.0) box -> public to private
0.1) make sure that static objects are initialized in proper order
0.2) documentation @file new lines
0.3) make SmartPtr.h header, strange behavior
